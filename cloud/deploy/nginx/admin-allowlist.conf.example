# Reference output only. The active file is generated from ADMIN_ALLOWED_IPS.
# Configure exact IPs in cloud/.env.staging, then recreate only nginx.
allow 127.0.0.1;
allow ::1;
# allow 203.0.113.10;
deny all;
