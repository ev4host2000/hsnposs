# Fail-closed reference only.
# The active /etc/nginx/admin-allowlist.conf is generated at container startup
# from ADMIN_ALLOWED_IPS by nginx-strict-entrypoint.sh. Do not add operator IPs here.
allow 127.0.0.1;
allow ::1;
deny all;
