/// Expense sync identifiers.
class ExpenseSyncConstants {
  ExpenseSyncConstants._();

  static const entityType = 'expense';
  static const scopeKey = 'expenses';
  static const pushPath = '/sync/push/expenses';
  static const pullPath = '/sync/pull/expenses';
}
